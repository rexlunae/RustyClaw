# Getting Started

For first-time setup, use:

- `docs/QUICKSTART.md` for the fastest path
- `docs/BUILDING.md` for feature flags and source builds

Typical flow:

```bash
cargo install rustyclaw
rustyclaw onboard
rustyclaw tui
```
